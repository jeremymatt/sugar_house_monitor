# Example Display Pi env file
# Copy to ../display_pi.env on the Pi and adjust URLs as needed.

DISPLAY_API_BASE=https://example.com/sugar_house_monitor/api
DISPLAY_REFRESH_SEC=15
# Settings scope for the display Pi history endpoint.
DISPLAY_HISTORY_SCOPE=display
# Optional fixed window override (seconds). Leave unset to use server display settings.
# DISPLAY_WINDOW_OVERRIDE_SEC=7200
# Max bins for history plots (display default).
NUM_PLOT_BINS=800
